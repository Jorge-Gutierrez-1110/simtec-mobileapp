<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">

        <item
            android:id="@+id/nav_home"
            android:icon="@android:drawable/ic_menu_compass"
            android:title="Inicio"
            android:checked="true"/>

        <item
            android:id="@+id/nav_register"
            android:icon="@android:drawable/ic_menu_camera"
            android:title="Registrar Asistencia"/>

    </group>

    <!-- MÓDULOS ADMIN -->
    <item
        android:id="@+id/navAdminHeader"
        android:title="Administración"
        android:visible="false">

        <menu>

            <item
                android:id="@+id/nav_nomina"
                android:icon="@android:drawable/ic_menu_agenda"
                android:title="Nómina"/>

            <item
                android:id="@+id/nav_solicitudes"
                android:icon="@android:drawable/ic_menu_edit"
                android:title="Centro de Aprobaciones"/>

            <item
                android:id="@+id/nav_expenses"
                android:icon="@android:drawable/ic_menu_manage"
                android:title="Control de Gastos"/>

            <item
                android:id="@+id/nav_monitor"
                android:icon="@android:drawable/ic_menu_view"
                android:title="Monitor en Vivo"
                android:enabled="false"/>

        </menu>

    </item>

    <!-- CUENTA -->
    <item android:title="Cuenta">

        <menu>

            <item
                android:id="@+id/nav_profile"
                android:icon="@android:drawable/ic_menu_myplaces"
                android:title="Mi Perfil"/>

            <item
                android:id="@+id/nav_logout"
                android:icon="@android:drawable/ic_lock_power_off"
                android:title="Cerrar Sesión"/>

        </menu>

    </item>

</menu>
